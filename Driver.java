import java.util.Scanner;

import Food.Delivery.ViewOrder;

public class Driver {
public static void main{String[] args) {
    Enum user {DRIVER, USER};
    user u = user.DRIVER;
    switch(u) {
        case DRIVER:
            ViewOrder vo = new ViewOrder();
            vo.Vieworder(fileName);
        }
}
                       }
